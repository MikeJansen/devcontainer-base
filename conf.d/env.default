#!/bin/bash

export DEVC_PROJECT="devc"
export DEVC_ID="1"
export DEVC_TAG="${DEVC_PROJECT}-${DEVC_ID}"
export AWS_PROFILE="default"
export AWS_REGION="us-east-2"

# base folders
export DEVC_CONF="${WORKSPACE}/conf.d"
export DEVC_BIN="${WORKSPACE}/bin"

# derived folders and files
export DEVC_CONF_LOCAL="${DEVC_CONF}/env.local.sh"

# Generated values
